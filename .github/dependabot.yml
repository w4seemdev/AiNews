# Keeps the SHA-pinned actions in .github/workflows fresh (see refresh-news.yml).
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
